<policies>
	<inbound>
		<base />
		<rate-limit-by-key calls="300" renewal-period="60" counter-key="@(context.Subscription?.Key ?? &quot;anonymous&quot;)" />
		<quota-by-key calls="250000" renewal-period="2628000" counter-key="@(context.Subscription?.Key ?? &quot;anonymous&quot;)" />
		<!-- No endpoint gating: Professional tier has access to all endpoints -->
	</inbound>
	<backend>
		<base />
	</backend>
	<outbound>
		<base />
	</outbound>
	<on-error>
		<base />
	</on-error>
</policies>
